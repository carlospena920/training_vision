model = YOLO("yolo11s-cls.pt")
    results = model.train(
        device=0,
        data=data,
        epochs=300,
        batch=16,
        imgsz=416,
        name=run_name,
        deterministic=True,

        # Optimización
        optimizer="AdamW",
        lr0=0.001,
        lrf=0.01,
        freeze=0,
        patience=50,
 
        # Augmentations realistas
        scale=0.2,
 
        hsv_h=0.02,
        hsv_s=0.15,
        hsv_v=0.15,
    )